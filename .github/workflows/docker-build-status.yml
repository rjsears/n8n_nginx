# -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
# .github/workflows/docker-build-status.yml
#
# Builds and pushes n8n_status Docker image
# to Docker Hub when n8n_status/ files change
#
# Richard J. Sears
# richard@n8nmanagement.net
# -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=

name: Build Status Collector Docker Image

on:
  # Trigger on push to v3 development branch when n8n_status files change
  push:
    branches:
      - refactor/frontend-optimization-v2  # v3 development branch
    paths:
      - 'n8n_status/**'
      - '.github/workflows/docker-build-status.yml'

  # Allow manual trigger from Actions tab
  workflow_dispatch:
    inputs:
      tag:
        description: 'Image tag (default: latest)'
        required: false
        default: 'latest'
      platforms:
        description: 'Target platforms'
        required: false
        default: 'linux/amd64,linux/arm64'

env:
  REGISTRY: docker.io
  IMAGE_NAME: rjsears/n8n_status

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      # =========================================
      # Checkout repository
      # =========================================
      - name: Checkout repository
        uses: actions/checkout@v4

      # =========================================
      # Set up QEMU for multi-arch builds
      # =========================================
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # =========================================
      # Set up Docker Buildx
      # =========================================
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # =========================================
      # Login to Docker Hub
      # =========================================
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # =========================================
      # Extract metadata for Docker
      # =========================================
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            # Always set latest tag
            type=raw,value=latest
            # Set tag from manual input (for workflow_dispatch)
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag != 'latest' }}
            # Set sha tag for traceability
            type=sha,prefix=sha-

      # =========================================
      # Build and push Docker image
      # =========================================
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./n8n_status
          file: ./n8n_status/Dockerfile
          push: true
          platforms: ${{ github.event.inputs.platforms || 'linux/amd64,linux/arm64' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_DATE=${{ github.event.repository.updated_at }}
            VCS_REF=${{ github.sha }}

      # =========================================
      # Output image digest
      # =========================================
      - name: Output image digest
        run: |
          echo "## Docker Image Built Successfully! :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ env.IMAGE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tags:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Platforms:** \`${{ github.event.inputs.platforms || 'linux/amd64,linux/arm64' }}\`" >> $GITHUB_STEP_SUMMARY
